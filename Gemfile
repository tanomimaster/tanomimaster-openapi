# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'committee'
gem 'openapi3_parser', github: 'kevindew/openapi3_parser.git', branch: 'openapi-3.1'

